import {API_VERSION, DB_DATABASE_NAME, DB_HOST, DB_PASSWORD, DB_USER} from './env';

export const BASE_API_ENDPOINT = `/api/${API_VERSION}`;
//export const DB_URI = `mongodb+srv://${DB_USER}:${DB_PASSWORD}@${DB_HOST}/${DB_DATABASE_NAME}?retryWrites=true&w=majority`;